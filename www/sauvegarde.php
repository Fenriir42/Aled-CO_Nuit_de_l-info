<?php phpinfo; 
   // $pdo->exec("SET CHARACTER SET utf8");

// $stmt = $pdo->prepare("SELECT * FROM sauveteur WHERE id=?");

// $stmt->bindParam(1,$id);

// $stmt->execute();
// $res = $stmt->fetchAll();
// foreach ( $res as $row ) {
//     echo $row['id'];
// }

    
    <?php phpinfo(); ?>
?>